import React from "react";
import SkillCard from "./SkillCard";

const SkillSection = () => {
  return (
    <div>
      {/* Grid area */}
      {/* 3 columns of skill */}
      <SkillCard />
    </div>
  );
};

export default SkillSection;
